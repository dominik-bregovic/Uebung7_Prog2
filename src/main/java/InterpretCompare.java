import java.util.Comparator;

public class InterpretCompare implements Comparator<AnalyseTable> {

    @Override
    public int compare(AnalyseTable o1, AnalyseTable o2) {
        return 0;
    }

//    public int compare(AnalyseTable o1, AnalyseTable o2) {
      /*  if (o1.getCountUniqueInterpretInList() > o2.getCountUniqueInterpretInList()) {
            return -1;
        }else if o1.getCountUniqueInterpretInList() < o2.getCountUniqueInterpretInList()) {
            return 1;
        }else
            return 0;
    }
    */

}
